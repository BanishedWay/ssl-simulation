package message;

public enum messageTypes {
    client_hello, server_hello, server_certificate, client_certificate, certificate_verify, client_key_exchange, server_finished, client_finished, error_message, application_data
}